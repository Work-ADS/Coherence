This app follows the repo-level Coherence instructions.

Before doing any planning, design, or implementation work from `apps/site/`, read:

1. `../../AGENTS.md`
2. `../../docs/strategy/plan.md`
3. The relevant agent harness under `../../docs/agents/`
4. The required rule files listed in `../../AGENTS.md`

Do not treat this app as a standalone Angular project. It is a consumer and documentation surface for the Coherence design system, so the repo-level 3-file rule, token-only styling rule, doc-page-shell rule, and pre-flight checks all apply here.
