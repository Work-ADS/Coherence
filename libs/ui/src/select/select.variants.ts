/**
 * afi-select — variant logic lives in `select.component.scss` via BEM
 * modifiers (per `docs/rules/component-skill.md §6`). This file is kept
 * empty-of-class-strings to preserve the file shape for future tooling.
 */

export {}; // intentional — type exports live in select.types.ts
