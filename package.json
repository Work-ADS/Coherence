{
  "name": "coherence",
  "version": "0.1.0",
  "private": true,
  "description": "Coherence — AFI Design System",
  "workspaces": [
    "apps/site",
    "libs/tokens"
  ],
  "scripts": {
    "start": "npm run build:tokens && npm start --workspace coherence-site",
    "build": "npm run build:tokens && npm run build --workspace coherence-site",
    "build:tokens": "cd libs/tokens && node build.mjs",
    "dev": "npm run build:tokens && npm start --workspace coherence-site"
  }
}
